import {StyleSheet} from 'react-native';

export const chartStyles = StyleSheet.create({
  container: {
    width: 120,
    height: 120,
    borderWidth: 2,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
